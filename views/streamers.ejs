<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="apple-touch-icon" sizes="180x180" href="<%= locals.hostname || '' %>/fav/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= locals.hostname || '' %>/fav/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= locals.hostname || '' %>/fav/favfavicon-16x16.png">
    <link rel="manifest" href="<%= locals.hostname || '' %>/fav/site.webmanifest">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= locals.WebsiteTitleElementText %>
    </title>
    <link rel="stylesheet" type="text/css" href="<%= locals.CssUrl || 'stylesheet1.css' %>">
    <link rel="stylesheet" type="text/css"
        href="<%= locals.hostname + '/base_stylesheet.css' || 'localhost/base_stylesheet1.css' %>">
    <%- include('parts/metatags.ejs') %>
</head>

<body>
    <%- include('parts/header.ejs') %>
        <main>
            <div class="title-section">
                <h1>Sponsored</h1>
                <div class="line"></div>
            </div>
            <div class="streamerlist">

                <% locals.streamer_list.forEach(element=> {
                    %>
                    <a class="streamer" href="./<%= element.userUsername %>"
                        style="background-image: url('https://static-cdn.jtvnw.net/previews-ttv/live_user_<%= element.userUsername %>-440x248.jpg')">
                        <div class="streamer-bottom">
                            <div class="streamer-pfp">
                                <img src="<%= element.userProfileImageUrl %>" alt="<%= element.name %>">
                            </div>
                            <h3 class="streamer-title">
                                <%= element.userDisplayname %>
                            </h3>
                        </div>
                    </a>
                    <% }); %>
            </div>
            <div class="title-section">
                <div class="line"></div>
                <h1>Live now</h1>
            </div>
        </main>
        <%- include('parts/footer.ejs') %>
</body>

</html>